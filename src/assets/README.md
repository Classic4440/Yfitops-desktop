# Assets

Place build/runtime assets here.

- `icon.ico` — Windows application icon used by `electron-builder` (referenced in
  `electron-builder.yml`). A 256×256 multi-resolution `.ico` is recommended. If
  it is absent, electron-builder falls back to the default Electron icon, so the
  build still succeeds.
